using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Locomotion : MonoBehaviour {
    //animation, articulation
    //direction to target  - using signals (go faster, go slower, turn right, turn left...) of the steering behavior
    //motion is the result of the camera's visual perception
    //all steps of behavioral locomotion - (ICManager - all cameras)
    private Steering steering;
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
